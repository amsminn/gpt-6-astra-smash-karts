PORT=3001
# Optional: Discord application's public client ID, required at build time for Activities.
VITE_DISCORD_CLIENT_ID=
# Optional comma-separated additional allowed WebSocket origins in production.
ALLOWED_ORIGINS=
# Optional external game service when hosting the web client separately.
VITE_GAME_SERVER_URL=
